<h1>Últimas noticias</h1>
<%# <h3>Show more</h3> %>
<%# <%= link_to 'Ad details', newspaper_article_path %>


<div class="article-wrapper">
<% @articles.each do |article| %>  

<div class="article-item">
      <h2 class="article-headline">
        <a href="<%= article.url %>" target="_blank">
        <div class="article-item-border"></div>
          <%= article.headline %>
        </a> 
      </h2>

    <div class="article-source">
    <%= article.created_at.strftime("%F") %>
      Fuente:
      <% source = article.newspaper_id %>  
      <a href='<%= Newspaper.find(source).newspaperUrl %>'>
        <%= Newspaper.find(source).newspaperName %> 
      </a>
      
    </div>
  </div>
<% end %>
</div>

  <%= link_to :root do %>
    <%= svg('arrow1', class: 'day-before') %>  
    <h2 class="">Ayer</h2>  
  <% end %>