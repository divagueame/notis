<p id="notice"><%= notice %></p>

<h1>Newspapers</h1>

<table>
  <thead>
    <tr>
      <th>Newspapername</th>
      <th>Newspaperurl</th>
      <th>Newspapercssselector</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @newspapers.each do |newspaper| %>
      <tr>
        <td><%= newspaper.newspaperName %></td>
        <td><%= newspaper.newspaperUrl %></td>
        <td><%= newspaper.newspaperCssSelector %></td>
        <td><%= link_to 'Show', newspaper %></td>
        <td><%= link_to 'Edit', edit_newspaper_path(newspaper) %></td>
        <td><%= link_to 'Destroy', newspaper, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Newspaper', new_newspaper_path %>
